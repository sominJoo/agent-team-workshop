# Supabase 연결 정보 — Project Settings → API 에서 확인
#
# anon public 키만 사용한다. 브라우저에 노출되는 것을 전제로 만들어진 공개 키다.
# 같은 화면의 service_role 키는 모든 권한을 우회하므로 절대 넣지 않는다.
#
# 이 파일을 복사해 .env 로 만들고 값을 채운다 (.env 는 커밋되지 않는다).
NUXT_PUBLIC_SUPABASE_URL=
NUXT_PUBLIC_SUPABASE_ANON_KEY=
