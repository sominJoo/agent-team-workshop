# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# 디자인 프로토타입 원본 (루트 HTML, 파일당 약 11MB)
/*.html

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# 게임 데이터 평문 원본 — 저장소가 public이라 절대 커밋하면 안 된다.
# 커밋되는 것은 npm run encrypt 가 만든 app/data/games.enc.json(암호문)뿐이다.
/data/games.source.json

.claude